twig:
    default_path: '%kernel.project_dir%/templates'
#    paths:
#        '%kernel.project_dir%/custom/plugins/DashboardBundle/Resources/templates/dashboard': DashboardBundle

when@test:
    twig:
        strict_variables: true
